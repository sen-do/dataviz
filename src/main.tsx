import React from "react";
import ReactDOM from "react-dom/client";

// Entry point — full app implementation handled separately.
// This stub exists so the shadcn CLI can detect the Vite+React framework.
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>Epstein Files Network Analysis</div>
  </React.StrictMode>,
);
